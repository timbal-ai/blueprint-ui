# ============================================
# App Configuration
# ============================================
VITE_APP_TITLE=Timbal | Your App Name
VITE_APP_PORT=3737

# ============================================
# Authentication Configuration
# ============================================
# Auth uses the Timbal API at https://api.timbal.ai
# Access token is in-memory, refresh token is in localStorage.

# Enable auth (set to "true" to enable, or just configure methods below)
VITE_AUTH_ENABLED=true


# ============================================
# Authorization (restrict access to your org/project)
# ============================================
# When both are set, only users with access to this project can use the app.
# Without these, any Timbal user can log in.
VITE_TIMBAL_ORG_ID=your-org-id
VITE_TIMBAL_PROJECT_ID=your-project-id

# ============================================
# Timbal SDK Configuration
# ============================================
VITE_TIMBAL_BASE_URL=https://api.timbal.ai

# API key (not needed when using Timbal IAM with auth enabled)
# VITE_TIMBAL_API_KEY=your-api-key

# Timbal IAM - use auth session token instead of API key (defaults to true when auth is enabled)
# VITE_AUTH_TIMBAL_IAM=true
