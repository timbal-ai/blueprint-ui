# ============================================
# App Configuration
# ============================================
VITE_APP_TITLE=Timbal | Your App Name
VITE_APP_PORT=3737

# ============================================
# Authentication Configuration
# ============================================
# Auth is automatically enabled when both URL and KEY are provided
# To disable auth, simply remove or comment out these variables

# Auth provider type: 'supabase' | 'custom' | 'auth0' | 'firebase'
# Default: supabase
VITE_AUTH_PROVIDER=supabase

# Provider credentials (auth is enabled when both are set)
# - Supabase: url = project URL, key = anon key
# - Auth0: url = domain (e.g., your-tenant.auth0.com), key = client ID
# - Firebase: url = auth domain, key = API key
# - Cognito: url = user pool ID, key = client ID
VITE_AUTH_URL=https://your-project.supabase.co
VITE_AUTH_KEY=your-anon-key

# ============================================
# Authentication Methods
# ============================================

# Comma-separated list of enabled auth methods
# Available methods:
#   - email (or password, email-password) - Email/password authentication
#   - google, github, azure, apple, facebook - OAuth providers
# Default: email (if not set)
# Examples:
#   VITE_AUTH_METHODS=email,google,github
#   VITE_AUTH_METHODS=google,azure
#   VITE_AUTH_METHODS=email
VITE_AUTH_METHODS=email,google,github,azure

# ============================================
# Usage Examples
# ============================================

# Example 1: Supabase with email/password only (no OAuth)
# VITE_AUTH_PROVIDER=supabase
# VITE_AUTH_URL=https://your-project.supabase.co
# VITE_AUTH_KEY=your-anon-key
# VITE_AUTH_METHODS=email

# Example 2: Supabase with Google OAuth only (no email/password)
# VITE_AUTH_PROVIDER=supabase
# VITE_AUTH_URL=https://your-project.supabase.co
# VITE_AUTH_KEY=your-anon-key
# VITE_AUTH_METHODS=google

# Example 3: Auth0 with email/password + Google + GitHub
# VITE_AUTH_PROVIDER=auth0
# VITE_AUTH_URL=your-tenant.auth0.com
# VITE_AUTH_KEY=your-client-id
# VITE_AUTH_METHODS=email,google,github

# Example 4: Multiple OAuth providers only
# VITE_AUTH_METHODS=google,github,azure

# Example 5: Disable all auth (remove or comment out credentials)
# VITE_AUTH_URL=
# VITE_AUTH_KEY=
